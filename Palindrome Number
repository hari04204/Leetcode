class Solution {
    public boolean isPalindrome(int x) {
        char[] a = (""+x).toCharArray();
        for(int i=0, j = a.length-1; i<a.length&&j>-1;i++,j--){
            if(a[i]!=a[j]){
                return false;
            }
        } 
        return true;
    }
}
